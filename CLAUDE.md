# Test Project

## Token Optimization Notes
- Use Haiku agents (researcher, code-reviewer) for exploration tasks
- Use Sonnet agents (coder, tester) for implementation tasks
- Run /clear between unrelated experiments
- Run /compact when context gets large
